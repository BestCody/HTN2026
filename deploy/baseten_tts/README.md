# Charlie Kokoro TTS

This Truss runs the dedicated Kokoro 82M speech model and returns a 24 kHz mono
WAV in MoIRA's `audio_b64` contract. The deployment uses the pinned `af_heart`
voice from the pinned model snapshot.
